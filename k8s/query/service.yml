apiVersion: v1
kind: Service
metadata:
  name: query
  namespace: doc-assist
spec:
  selector:
    app.kubernetes.io/name: doc-assist
    app.kubernetes.io/component: query
  ports:
    - protocol: TCP
      port: 8002
      targetPort: 8002
